<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://bootswatch.com/4/minty/bootstrap.min.css" rel="stylesheet">
    <title>Express App</title>
</head>
<body>
    <h1>My express notes app</h1>
    <h3>By Diana Villalvazo</h3>
    <div class="container">
        <div class="row">
            <div class="col-md-5">
                <div class="card">
                    <div class="card-body">
                        <form action="/add" method="POST">
                            <div class="form-group m-2">
                                <input type="text" placeholder="Title" name="title" class="form-control">
                            </div>
                            <div class="form-group m-2">
                                <input type="text" placeholder="Message" name="message" class="form-control">
                            </div>            
                            <button class="btn btn-primary" type="submit">Add</button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
    
    <div class="container">
        <table class="table table-hover">
            <thead>
            <th>Title</th>
            <th>Message</th>
            </thead>
            <tbody>
                <% if (data) { %>
                    <% for (var i=0; i<data.length; i++) { %>
                        <tr>
                        <td><%= data[i].title %></td>
                        <td><%= data[i].message %></td>
                        <td>
                            <a href="/edit/<%= data[i].idnotes %>" class="btn btn-info">Edit</a>
                            <a href="/delete/<%= data[i].idnotes %>" class="btn btn-secondary">Delete</a></td>
                        </tr>
                    <% } %>
                <% } %>
            </tbody>
        </table>
    </div>
</body>
</html>